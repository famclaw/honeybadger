id: sc-hardcoded-ip
kind: pattern
scanner: supplychain
category: network
severity: HIGH
message: Hardcoded IP endpoint
patterns:
  - regex: 'https?://(?:\d{1,3}\.){3}\d{1,3}(?::\d+)?/'
    description: URL with hardcoded IP address
